import uglify from 'rollup-plugin-uglify';

export default {

	input: "dist/ig_screenshot.js",
	extend: true,

	plugins: [

		uglify()
	],
	output: [{
		file: "dist/ig_screenshot.min.js",
		format: "umd",
		exports: 'named',
		name: "screenShooter",
		sourcemap: true
	}]
};
